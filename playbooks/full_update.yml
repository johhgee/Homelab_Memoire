---
- name: MAJ système
  hosts: all
  become: true
  tasks:
    - name: Apt update + Apt upgrade
      ansible.builtin.apt:
        update_cache: true
        upgrade: true
        autoremove: true
